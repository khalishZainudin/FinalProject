<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.edu.fsktm.um.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowProfile"></activity>
        <activity android:name=".Bottleneck.BottleneckActivity" />
        <activity android:name=".PSUCalculatorPage.PSUCalculatorActivity" />
        <activity android:name=".LoginPage.ResetPasswordActivity" />
        <activity android:name=".LoginPage.ResetPasswordActivity" />
        <activity android:name=".CompatibilityPage.CompatibleMainActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginPage.UpdateProfileActivity" />
        <activity android:name=".ForumPage.AddForum" />
        <activity android:name=".ForumPage.ForumInterface" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".LoginPage.EmailPassActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".ForumPage.ForumPage" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>